import { GeokbdDirective } from './geokbd.directive';

describe('GeokbdDirective', () => {
  it('should create an instance', () => {
    const directive = new GeokbdDirective();
    expect(directive).toBeTruthy();
  });
});
